AR = ar
CC = gcc
CFLAGS=%CFLAGS% -fPIC -Wall -g -c -lzlib
LDFLAGS=%LDFLAGS%

all: 
	$(CC) $(CFLAGS) pnglite.c
	$(CC) $(LDFLAGS) -shared -Wl,-soname,libpnglite.so.0 -o libpnglite.so.0.1 pnglite.o

clean:
	rm -f *.so.*
